services:

  web:
    build: ./docker/web
    environment:
      - APACHE_RUN_USER=#1000
    volumes:
      - ${WEB_ROOT_PATH:-.}:/var/www/html
    working_dir: /var/www/html
    ports:
      - ${WEB_PORT:-80}:80
